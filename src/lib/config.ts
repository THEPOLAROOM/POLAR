/**
 * Solo-barber V1: the one configured POLAR barber account. No barber
 * directory/search exists or is planned for V1 — every barber-facing
 * booking read/write uses this fixed profile id rather than resolving
 * it implicitly from data a client happens to be able to read.
 */
export const POLAR_BARBER_PROFILE_ID = "2114af90-59a1-4c44-b664-d801cec10598";
